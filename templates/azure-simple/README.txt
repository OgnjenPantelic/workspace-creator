Open terminal in the folder that you just downloaded

1. Copy and paste the following in the terminal to create a virtual environment:
python3 -m venv venv
source venv/bin/activate

2. Copy and paste the following in the terminal to install the necessary requirements:
pip install -r requirements.txt

3. Copy and paste the following in the terminal to run the application:
python deploy.py
