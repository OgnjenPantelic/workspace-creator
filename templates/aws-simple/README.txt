Open terminal in the folder that you just downloaded

1. Create Virtual Environment by typing this in terminal:
python3 -m venv venv
source venv/bin/activate

2. Then type this in terminal to install necessary requirements:
pip install -r requirements.txt

3. Type this to run the application:
python deploy.py
